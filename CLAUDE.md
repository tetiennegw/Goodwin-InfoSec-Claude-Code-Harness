# CLAUDE.md — {{harness.name}} (Template — Pre-Onboarded)

This template repo has not yet been onboarded. The agent identity, doctrine, and team context will be populated by `/onboard` on first session.

> **First-time setup required.** Open Claude Code in this directory and run `/onboard` to:
> - Capture your identity (name, email, role, manager)
> - Capture your team context (peers, lead tools)
> - Personalize CLAUDE.md, INDEX.md, .claude/settings.local.json, memory/MEMORY.md
> - Create required directories (notes/YYYY/MM/, ingest/, hub/staging/, hub/state/, memory/)
> - Seed lifecycle anchor files (LEDGER-APPEND-ANCHOR, PREPEND-ANCHOR:v1, etc.)
> - Optionally install/validate dependencies (gh CLI, Codex, Neo, Planner module, signing cert)
> - Stamp `.harness-onboarded` sentinel

After onboarding, this file is replaced with the full {{harness.name}} identity + doctrine sourced from the canonical Goodwin InfoSec template.

For more information about {{harness.name}}, see `docs/README.md` and `docs/morpheus-features/`.
